import styled from '@jss';

const Label = styled(
  'label',
  {
    base: {
      display: 'inline-block',
      margin: 0,
      padding: 0,
      fontWeight: 700,
    },
  },
);

Label.displayName = 'Label';

export default Label;
